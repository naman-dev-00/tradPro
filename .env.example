# Application Environment (local, test, staging, production)
APP_ENV=local

# Database Connection (Optional for local/test; defaults to local SQLite)
# For local PostgreSQL with Docker Compose:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tradepro

# Next.js API Base URL (Frontend)
NEXT_PUBLIC_API_URL=http://localhost:8000
